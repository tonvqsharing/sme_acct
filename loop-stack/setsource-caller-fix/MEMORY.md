# Loop Memory
Updated continuously by all agents as they discover things.
## Learnings
- [G1] 2026-09-22: locked Option 2 domain-only in docs/SetSource-CallerFix-Design-2026.md — 3 SetSource guards (posted/type/id, verbatim messages) + 1 caller fail-fast `Id<=0` pre-condition before JE build; line 78 literal unchanged; 2-file touch boundary; discard bug + null/null path + PostingReference + EF explicitly no-touch; design-only, zero code diff.
- [G1] 2026-09-22: verifier VERIFIED_PASS — all messages verbatim vs source (JournalEntry.cs:47-48, PostingReference.cs:21-24), fail-fast position confirmed (:75 after balance check, before JE build :77), PLAN.md [G1]/[G2] paths updated to docs file (auditor WARN resolved), STATUS heartbeats consolidated. G2 next: TDD facts (a)-(i) first, then guards per design §3/§4.
- [G1] 2026-09-22: why domain-only Option 2 wins — handler path loads period via GetByIdAsync (PostOpeningBalancesHandler.cs:16) so :78 `SetSource("OpeningBalance", Id)` already carries a persisted Id in production; the Id=0 hazard fires only for transient never-saved in-memory posts, so the fail-fast pre-condition fixes only the broken pre-save path with zero handler/DI/EF churn. Option 1 = discarded-JE bug (separate task per design §9 item 3); Option 3 = relaxed guard contradicts design §5 normative `sourceId > 0` + PostingReference V4 parity.
- [G1] 2026-09-22: guard-ordering safety confirmed by verifier — SetSource (:78) runs before journalEntry.Post (:85) so the new reject-once-posted guard never fires on the OB path; transient-Id=0 fail-fast fires at :75 before JE build (:77) so no accidental V4 trip (fact f vs g separation).
