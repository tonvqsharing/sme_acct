# Loop Plan
## Mode
patch
## Goal
Implement CreateJournalEntryCommand and PostJournalEntryCommand handlers so the Phiếu kế toán daily screen works end-to-end: creating a balanced voucher persists via IJournalEntryRepository (numbering series wired), posting marks it posted via domain Post() + IClock + IUnitOfWork; RED-GREEN, gates green (build 0/0, arch 22/22, bank >=61)
## Stop Condition
all tasks in loop-stack/journal-entry-handlers/PLAN.md checked
## Budget
20 turns
## Git Integration
yes
## Tasks

- [x] [G1] CompanyId plumbing (Application + Api, 5 files, build stays green): add `long CompanyId` as FIRST parameter of `CreateJournalEntryCommand` (src/SmeAccounting.Application/Commands/CreateJournalEntryCommand.cs); add `RuleFor(x => x.CompanyId).GreaterThan(0)` to `CreateJournalEntryCommandValidator` (src/SmeAccounting.Application/Validators/); add `[Required] long CompanyId` + Display("Mã công ty") to `CreateJournalEntryViewModel` (src/SmeAccounting.Api/ViewModels/); add required number input `asp-for="CompanyId"` to Create.cshtml (src/SmeAccounting.Api/Views/JournalEntry/); pass `model.CompanyId` as first arg in JournalEntryController.Create POST (src/SmeAccounting.Api/Controllers/). No claims/session — form field only. Verify: `dotnet build SmeAccounting.sln` 0 warnings/0 errors; `dotnet test tests/SmeAccounting.ArchitectureTests/` 22/22; `dotnet test tests/SmeAccounting.BankTests/` still 61 (no behavior change). No migration, no Domain/Infrastructure edits.
- [x] [G2] CreateJournalEntryHandler RED-GREEN: RED first — add `FakeVoucherTypeRepository` (List-backed, `GetByCodeAsync` filters Code+CompanyId, Stored) + `FakeDocumentNumberingSeriesRepository` (List-backed, `GetDefaultAsync` filters VoucherTypeId+CompanyId+IsDefault, Stored) to tests/SmeAccounting.BankTests/Fakes.cs; new tests/SmeAccounting.BankTests/CreateJournalEntryHandlerTests.cs with validator facts (valid; CompanyId 0 fails; PeriodId 0 fails; empty Lines fails) + handler happy path (construct `new CreateJournalEntryHandler(voucherTypeRepo, seriesRepo, jeRepo, uow)` directly, assert Stored[0].EntryNumber == "JNRL000001" from Prefix "JNRL"/NextNumber 1/PaddingLength 6, series.NextNumber incremented to 2, result.Id == Stored[0].Id, SaveCalledCount == 1) + null-guard facts (missing VoucherType "JNRL" → InvalidOperationException; missing default series → InvalidOperationException) — run `dotnet test tests/SmeAccounting.BankTests/` and record compile-error RED (handler missing). GREEN — new src/SmeAccounting.Application/Handlers/CreateJournalEntryHandler.cs: internal sealed, primary ctor (IVoucherTypeRepository, IDocumentNumberingSeriesRepository, IJournalEntryRepository, IUnitOfWork); `GetByCodeAsync("JNRL", request.CompanyId)` → null → InvalidOperationException; `GetDefaultAsync(voucherType.Id, request.CompanyId)` → null → InvalidOperationException; entryNumber = `$"{series.Prefix}{series.NextNumber:D{series.PaddingLength}}"`; `series.Increment()`; `new JournalEntry(entryNumber, request.Date, request.PeriodId, request.Description)`; AddLine per input with `new Money(amount, "VND")`; AddAsync; SINGLE SaveChangesAsync (entry + series atomically); return `new CreateJournalEntryResult(entry.Id, entry.EntryNumber)`. Verify: bank tests green (61 + new), build 0/0, arch 22/22.
- [ ] [G3] PostJournalEntryHandler RED-GREEN: RED first — add `FakeClock : IClock` (settable `Now`) to tests/SmeAccounting.BankTests/Fakes.cs; new tests/SmeAccounting.BankTests/PostJournalEntryHandlerTests.cs: validator fact (JournalEntryId 0 fails) + handler happy path (build JournalEntry via ctor + AddLine, AddAsync to FakeJournalEntryRepository so Id assigned, Handle `PostJournalEntryCommand(entry.Id)` with clock.Now fixed, assert IsPosted true, PostedAt == clock.Now, result.JournalEntryId == entry.Id, SaveCalledCount == 1) + already-posted fact (second Post → DomainException) + not-found fact (unknown id → InvalidOperationException) — run `dotnet test tests/SmeAccounting.BankTests/` and record compile-error RED (handler missing). GREEN — new src/SmeAccounting.Application/Handlers/PostJournalEntryHandler.cs: internal sealed, primary ctor (IJournalEntryRepository, IUnitOfWork, IClock); `GetByIdAsync(request.JournalEntryId)` → null → InvalidOperationException; `entry.Post("system", _clock.Now)` (fixed postedBy string, IClock for postedAt — do NOT inject IPostingService, dead port); SaveChangesAsync; return `new PostJournalEntryResult(entry.Id, entry.PostedAt)`. Verify: bank tests green (61 + new), build 0/0, arch 22/22.
- [ ] [G4] Integration gates + no-migration/no-touch evidence: `dotnet build SmeAccounting.sln` 0 warnings/0 errors; `dotnet test tests/SmeAccounting.ArchitectureTests/` 22/22; `dotnet test tests/SmeAccounting.BankTests/` ≥ 61 (record exact count). No-migration evidence: Migrations dir file count unchanged, latest migration timestamp (20260922084832_PostingReferenceHarden) predates loop commits, zero NEW migration files. No-touch audit via `git show --stat` per commit: no Domain/Infrastructure schema files, no IPostingService, no JournalEntry entity changes. Record all evidence in STATUS.md.
