# Loop Memory
Updated continuously by all agents as they discover things.
## Learnings
- [G1] 2026-09-22: locked Option 2 domain-only in docs/SetSource-CallerFix-Design-2026.md — 3 SetSource guards (posted/type/id, verbatim messages) + 1 caller fail-fast `Id<=0` pre-condition before JE build; line 78 literal unchanged; 2-file touch boundary; discard bug + null/null path + PostingReference + EF explicitly no-touch; design-only, zero code diff.
- [G1] 2026-09-22: verifier VERIFIED_PASS — all messages verbatim vs source (JournalEntry.cs:47-48, PostingReference.cs:21-24), fail-fast position confirmed (:75 after balance check, before JE build :77), PLAN.md [G1]/[G2] paths updated to docs file (auditor WARN resolved), STATUS heartbeats consolidated. G2 next: TDD facts (a)-(i) first, then guards per design §3/§4.
- [G1] 2026-09-22: why domain-only Option 2 wins — handler path loads period via GetByIdAsync (PostOpeningBalancesHandler.cs:16) so :78 `SetSource("OpeningBalance", Id)` already carries a persisted Id in production; the Id=0 hazard fires only for transient never-saved in-memory posts, so the fail-fast pre-condition fixes only the broken pre-save path with zero handler/DI/EF churn. Option 1 = discarded-JE bug (separate task per design §9 item 3); Option 3 = relaxed guard contradicts design §5 normative `sourceId > 0` + PostingReference V4 parity.
- [G1] 2026-09-22: guard-ordering safety confirmed by verifier — SetSource (:78) runs before journalEntry.Post (:85) so the new reject-once-posted guard never fires on the OB path; transient-Id=0 fail-fast fires at :75 before JE build (:77) so no accidental V4 trip (fact f vs g separation).
- [G2] 2026-09-22: TDD core landed — RED first (4/9 new Facts failed: b,c,d,f guard-dependent; 5 passed as unchanged-behavior), then GREEN: JournalEntry.SetSource 3 guards (IsPosted→type→id, verbatim messages, DomainException only, setter stays setter) + OpeningBalancePeriod.PostOpeningBalances Id<=0 fail-fast at new :75 (after balance check, before JE build); line 78 SetSource("OpeningBalance", Id) verbatim. Touch: exactly 2 production files (JournalEntry.cs +6, OpeningBalancePeriod.cs +3) + 2 test files (JournalEntrySourceTests.cs 9 Facts, Fakes.cs +FakeOpeningBalancePeriodRepository). Gates: build 0/0, BankTests 54/54 (45 baseline + 9 new), arch 22/22. No migration (domain in-memory guards + caller reorder only).
- [G2] test-discovery gotchas: BaseEntity.Id has PUBLIC setter — tests set persisted Id directly (period.Id = 5), no reflection/subclass needed; PostOpeningBalancesHandler internal + InternalsVisibleTo("SmeAccounting.BankTests") already in Application.csproj — handler Facts testable with zero csproj edits; handler namespace SmeAccounting.Application.Handlers needs explicit using (CS0246 otherwise); fact (i) assert-and-record via reflection over handler ctor params (no IJournalEntryRepository) — documents discard state, fails when discard-bug task lands (intentional per design §5).
- [G2] 2026-09-22: verifier VERIFIED_PASS — V1 guards verbatim (JournalEntry.cs:32-42, OpeningBalancePeriod.cs:76-77, :81 SetSource verbatim), facts (a)-(i) all present with edge variants (null/""/"  ", 0/-1, transient asserts state unchanged, persisted Id=5 passthrough), commit 5b3db2c scoped to 2 prod + 2 test files, gates independently re-run green (build 0/0, arch 22/22, BankTests 54/54). G3 next: formal verify core→edge + no-migration statement.
- [G2] 2026-09-22: auditor CLEAN — SetSource 3 guards + caller fail-fast verbatim per design §3/§4 (order IsPosted→type→id, setter stays setter, DomainException only, signature unchanged), commit 5b3db2c scoped to exactly 2 prod + 2 test files, no handler/DI/EF/CQRS/PostingReference/migration changes, no TODOs. G2 fully closed; G3 (verify core→edge + no-migration statement) is the only remaining task.
- [G3] 2026-09-22: verify core→edge PASS, zero code changes by executor — exact gate tails: (1) `dotnet build SmeAccounting.sln` → "Build succeeded. / 0 Warning(s) / 0 Error(s) / Time Elapsed 00:00:40.98" exit 0; (2) `dotnet test tests/SmeAccounting.ArchitectureTests/` → "Passed!  - Failed:     0, Passed:    22, Skipped:     0, Total:    22, Duration: 4 s - SmeAccounting.ArchitectureTests.dll (net10.0)" exit 0; (3) `dotnet test tests/SmeAccounting.BankTests/` → "Passed!  - Failed:     0, Passed:    54, Skipped:     0, Total:    54, Duration: 3 s - SmeAccounting.BankTests.dll (net10.0)" exit 0 (45 baseline + 9 new SetSource/caller Facts all green, 0 skipped).
- [G3] 2026-09-22: no-migration statement — domain in-memory guards + caller fail-fast reorder only; no new table/column/index/FK/DbContext change. Evidence: `src/SmeAccounting.Infrastructure/Migrations/` holds 17 migrations, latest `20260922084832_PostingReferenceHarden` (prior loop, predates this loop's commits 66808a0/5b3db2c/77bda12) — no setsource-caller-fix migration scaffolded. Guards live in `JournalEntry.SetSource` + `OpeningBalancePeriod.PostOpeningBalances` (runtime in-memory checks, no schema surface); SourceType/SourceId columns already exist on journal_entries (nullable read-model cache from InitialCreate-era schema).
- [G3] 2026-09-22: no-migration evidence technique generalizes for future domain-only loops — prove zero schema surface via Migrations-dir file count (35 files = 17 migrations ×2 + snapshot) + latest migration timestamp predating loop commits + `grep -i <feature>` over Migrations dir = 0 hits; runtime in-memory guards (entity-method checks) touch no table/column/index/FK/DbContext, so the no-migration statement passes audit/verify without scaffolding anything.
- [G3] 2026-09-22: loop close — handoff pointers for future loops: (1) discarded-JE persist bug (`PostOpeningBalancesHandler` has zero `IJournalEntryRepository.AddAsync`; design §9 item 3) — when fixed, fact (i) test (asserts no IJournalEntryRepository via reflection over handler ctor params) will FAIL intentionally and must be updated/removed in the same change; (2) dead null/null command path (`CreateJournalEntryCommand` optional SourceType/SourceId + `JournalEntryController.cs:39-41` hardcoded nulls, no handler; §9 item 4) — this loop's SetSource guards already cover its future inputs, so resurrecting it needs no further guard work.
