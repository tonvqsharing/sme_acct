# Loop Status
## State
IN_PROGRESS
## Current Task
G5: Final reconciliation matrix + DoD + report (COMPLETE)
## Task Progress
11 / 11 complete
## Attempts On Current Task
2
## Completed Tasks
- G1: Design reconciliation for 8 missing inventory entities (VERIFIED_PASS)
- G2: UomClass vertical slice (VERIFIED_PASS — 24/24 focused, gates 0/0+22/22+217/217)
- G2: ItemGroup vertical slice (VERIFIED_PASS — 19/19 focused; snapshot diff anomaly = partners WIP, resolved)
- G2: ItemBarcode vertical slice (VERIFIED_PASS — 18/18 focused, independently re-run all gates)
- G2: PriceList + ItemPriceList vertical slice (VERIFIED_PASS — 41/41 focused)
- Commit cbbe310: G2 code (96 files, 3177 insertions; co-traveling partners hunks in 3 shared files documented)
- G3: ItemReorderLevel vertical slice (VERIFIED_PASS — 19/19 focused, build 0/0)
- G3: ItemSupplierPrice vertical slice (VERIFIED_PASS — 26/26 focused, build 0/0, config: full unique 5-col NO HasFilter, Option A currency, handler deps repo+IUnitOfWork only)
- G3: ItemTaxClass vertical slice (VERIFIED_PASS — 19/19 focused, build 0/0, config: full unique 4-col NO HasFilter, effective-dating, handler deps repo only)
- G3: WarehouseLocation vertical slice (VERIFIED_PASS — 20/20 focused, build 0/0, config: full unique (CompanyId,WarehouseId,Code) NO HasFilter, BankBranch precedent, handler deps repo only)
- G4: Migration #19 AddInventoryModule03 (VERIFIED_PASS — build 0/0, arch 22/22, BankTests 301/301, migration reviewed: 9 tables + 2 AddColumn + 4 HasFilter partial uniques, all FKs Restrict, snake_case, xmin, decimal(18,2/3))
- G5: Final reconciliation matrix + DoD + report (COMPLETE)
## Skipped Tasks
(none)
## Last Researcher Result
G3 ItemSupplierPrice slice research complete — spec returned to orchestrator for append under "## Task-Specific Research — ItemSupplierPrice Slice". Closest live template = ItemPriceList (entity guards/validator/config/command/DTO/controller byte-shape) + SupplierItemConfiguration (3-FK Restrict) + TaxRate full-unique versioning key (CompanyId,SupplierId,ItemId,CurrencyCode,EffectiveFrom) NO HasFilter (R7 effective-dated — deactivate→re-add same key collides, accepted). Option A currency locked (ctor rejects empty/2/4-char + ToUpperInvariant normalize; validator Matches("^[A-Z]{3}$") rejects lowercase). §11 task 7 touches NOTHING existing → SupplierItem/Supplier/Item untouched, handler = IItemSupplierPriceRepository + IUnitOfWork only (NO IItemRepository/ISupplierRepository — 4 members each read; FakeItemRepository exists :325, FakeSupplierRepository does NOT and is not needed). Wiring post-ItemReorderLevel: DbSet after :46, Ignore after :108, DI after :69, Fakes after :405 (re-read before edit — parallel batch race). 15 CREATE + 3 wiring EDITs, existing-entity EDIT none; 26 core Facts (+2 optional Get-handler) → baseline 236 → 262. NO migration, NO csproj, NO ViewModel/View.
G2 PriceList slice research complete (RESEARCH.md "## Task-Specific Research — PriceList Slice" appended). Two entities ONE slice confirmed: PriceList = Uom flat-master template (4-member port, FakeCustomerGroupRepository shape); ItemPriceList = SupplierItem port shape (3 members) + TaxRate effective-dating/unique (CompanyId,PriceListId,ItemId,CurrencyCode,EffectiveFrom) + SupplierItemConfiguration 3-FK Restrict. Currency = string CurrencyCode(3) CONFIRMED (ExchangeRate.cs:9-11 string codes + :40-41 ToUpperInvariant; Money.cs:13 ArgumentNullException = R23, no FK to currencies). ⚠️ Canonical §3b "currencyCode lowercase throws" contradicts ctor guard "normalize ToUpperInvariant" — RESOLVED Option A: ctor rejects empty/2-char/4-char + normalizes lowercase; validator rejects lowercase via Matches("^[A-Z]{3}$") (G3 sibling :907 already says "invalid"; R23 says format at Application layer). Wiring: DbContext DbSets after :40 (Items), Ignore after :96 (ItemCreated), DI after :63 (IItemRepository). Fakes: FakePriceListRepository (4-member) + FakeItemPriceListRepository (3-member, FakeSupplierItemRepository shape); no FakeItemRepository needed. Cross-field Must precedent = CreateUomConversionCommandValidator.cs:14. Deactivate handlers THROW InvalidOperationException (SupplierItem pattern, not Item return-false). ~26 CREATE + 3 EDIT, NO migration, NO csproj edits.
G2 ItemGroup slice research complete (RESEARCH.md "## Task-Specific Research — ItemGroup Slice" appended). Templates confirmed: Uom slice = flat-master copy source; SupplierGroupAggregateTests = test template (AGENTS.md's ItemAggregateTests.cs does NOT exist — stale ref). Item ctor = 8 params now; ItemGroupId append = 9th param LAST, 7-file surface (Item.cs, CreateItemCommand, CreateItemCommandValidator, CreateItemHandler, ItemDto, GetItemHandler, ItemConfiguration) — canon §1 omits CreateItemHandler pass-through (DISCREPANCY #1: silent-null risk, same gap as §7b). Deactivate handler must THROW on missing (SupplierGroup precedent) not return-false (Item precedent) — DISCREPANCY #3. ItemController.Create optional itemGroupId param — beyond canon, flagged. C1 confirmed: ItemCategory has ParentId self-ref, ItemGroup flat no ParentId, no hierarchy validation. DbContext: DbSet after :40 + Ignore after :96; DI AddScoped after :63. 18 Facts spec'd (14 ItemGroup + 4 Item-append; trap: stock Item ctor requires uomId). NO migration (G4 owns AddColumn).
## Last Executor Result
G1 design reconciliation COMPLETE — "## Design Decisions (G1)" appended to loop-stack/implement-module-inventory/RESEARCH.md (after line 616). Contents: §0 cross-cutting canon (uniqueness classification R7, FK-Restrict rule R3/R4/R6, R1/R2/R14/R17/R23/R24/R32 dispositions, migration #19 shape = 9 CreateTable + 2 nullable AddColumn NO defaultValue + 4 HasFilter partial uniques); §1–§8 per-entity design blocks for all 8 entities (ItemGroup, ItemBarcode/GTIN, PriceList+ItemPriceList, ItemReorderLevel, ItemSupplierPrice, ItemTaxClass, UomClass, WarehouseLocation) — each with table name, company scope + ownership, exact field list (C# type/precision/length/nullable/default), exact unique constraints (composite + HasFilter expressions), FK list all Restrict, effective-dating/IsActive/soft-delete, minimal events, port members + repo behavior, command records (CompanyId FIRST) + validator rules (20/200/500), DTO + query shapes, controller routes, BankTests list (positive/negative/company-isolation/deactivation); §7b full 6-file UomClassId append surface transcribed verbatim from verified source (Uom.cs ctor param LAST + HasValue&&<=0 guard, CreateUomCommand, validator, UomDto, GetUomHandler Map, UomConfiguration) + CreateUomConversionHandler class-compat check; GTIN design block (BarcodeType enum GTIN8/12/13/14+Other, GS1 Mod-10 weights 3/1 right-to-left algorithm with verified test vectors 6291041500213/5012345670003, validator-only location, partial-unique is_active on (CompanyId,Barcode) + is_primary on (CompanyId,ItemId), UomId? nullable); §9 C1–C5 canonical citations; §10 R1–R32 disposition matrix (each addressed or documented limitation + why); §11 dependency map (slice → builds → references). All cited source files re-read by executor before transcription (Uom/Supplier/Item/ItemCategory/SupplierItem/BankBranch/TaxRate/TaxType/Warehouse/ExchangeRate/InventoryValuationPolicy entities, Uom 6-file slice, Item slice, SupplierItem/TaxRate/BankBranch/UomConversion configs, ports, Fakes.cs, SupplierItemAggregateTests). Zero src/ changes, zero migrations, zero git staging (G1 is loop-state only). Verifier: check §9/§10 against R-numbers and cited files.
## Last Audit Result
CLEAN — G2 UomClass slice. §7b 7-file append all MATCH (ctor param LAST `long? uomClassId = null`, guard `HasValue && <= 0`, property before IsActive, validator `GreaterThan(0).When(HasValue)`, CreateUomHandler pass-through present, DTO/Map/Configuration correct incl. HasOne<UomClass> Restrict). Class-compat null-safe (no NRE; both-differ→DomainException, either-unclassified→allowed, both-unclassified→allowed). 16 new files pattern-compliant (private ctor, DomainException guards, minimal event, 4-member port, tracked/AsNoTracking repo, internal handlers, thin controller, FluentValidation 20/200/500). Fakes match real ports exactly (FakeUomConversionRepository uses GetByCompanyAsync — trap avoided). Wiring: DbSet+Ignore both adjacent to Uom lines, DI adjacent, no duplicates; foreign lines = parallel slices only. Tests: 19 UomClass Facts + 4 class-compat Facts, Id=5 convention, SaveCalledCount asserts. No WARN/BLOCK.
---
WARN — G2 ItemGroup slice (informational only, no code fix): (1) DISCREPANCY #2: ItemController.Create NOT extended with optional itemGroupId param (controller still 8 params) — research-sanctioned documented limitation (usable via command only); itemGroupId UNREACHABLE from Item create form. RECORD in G5 report. Same question pending for UomController (UomClass slice). (2) RESEARCH.md summary "18 Facts" vs detailed spec 19 — summary text typo only, implementation matches detailed spec (19). Gate note: auditor did NOT independently re-run gates (steps exhausted; ItemBarcode auditor re-ran 0/0 + 22/22 + 217/217 + 18/18, orchestrator ran build 0/0 + arch 22/22 + BankTests 217/217 centrally). Verified CLEAN: C1 flat master (no ParentId; ItemCategory retains self-ref), 7-file Item append all correct (DISCREPANCY #1 fixed — CreateItemHandler passes request.ItemGroupId as 9th arg), DISCREPANCY #3 fixed (DeactivateItemGroupHandler throws InvalidOperationException on missing, command shape (long Id) + Result(bool) matches DeactivateItemCommand), 19 Facts match spec §6, Fakes exact port shapes, wiring adjacent, no migration scaffolded.
---
CLEAN — G2 ItemBarcode slice (audited 2026-09-24). All 6 adversarial checks pass, gates independently re-run (build 0/0, arch 22/22, BankTests 217/217, ItemBarcode filter 18/18):
1. **GS1 Mod-10 CORRECT** — `HasValidCheckDigit` verified by hand for all 4 GTIN types: weights `(dataLength-1-i)%2==0?3:1` = 3/1 right-to-left with 3 on rightmost data digit; length-match per type (7/11/12/13 data + 1); non-digit → false; `(10-(sum%10))%10`; `.When(BarcodeType != Other)`. Recomputed: 6291041500213 (sum=57, cd=3 ✓), 5012345670003 (sum=57, cd=3 ✓), negative 6291041500214 (cd=3≠4 ✓), plus well-known GTIN-8 12345670 (cd=0), GTIN-12 012345678905 (cd=5), GTIN-14 00012345678905 (cd=5) all ✓. Unknown (BarcodeType)999: IsInEnum fails AND Must rule runs (999≠Other) with switch-default dataLength=0 → false, no throw — test #13 green.
2. **Two-flag partial unique EXACT** — `HasFilter("\"is_active\"")` on (CompanyId,Barcode) + `HasFilter("\"is_primary\" AND \"is_active\"")` on (CompanyId,ItemId); filter strings match HasColumnName is_active/is_primary; Deactivate() sets IsActive=false only, never touches IsPrimary (Fact #18 locks rationale).
3. **Deactivate handler THROWS InvalidOperationException on missing** (SupplierItem precedent); BarcodeType enum-in-controller = PaymentMethod precedent byte-for-byte (`using Domain.ValueObjects` + `Enum.Parse<BarcodeType>`) — arch-legal (LayerCoupling bans only Entities/Ports).
4. **Pattern compliance** — entity guards all DomainException (no Enum.IsDefined, validator-only per PaymentMethod precedent), minimal event ItemBarcodeCreated(Id,CompanyId,OccurredOn), port 3 members (no GetByCodeAsync), tracked GetById/AsNoTracking GetAllByCompany/AddAsync repo, internal handlers, controller thin (ValueObjects only), CompanyId FIRST, DTO enum-as-string, one handler class both queries + private static Map.
5. **Fakes match port exactly** — FakeItemBarcodeRepository 3 members + Stored; plain Add (no counter — correct, ctor has no own-Id guard); Id=5 before AddAsync in deactivate-happy; SaveCalledCount==1 asserted.
6. **18/18 Facts** — all spec'd cases present incl. company isolation + Fact-18 Deactivate-leaves-IsPrimary proxy; zero TODOs (grep exit 1).
Wiring: DbSet :43 + Ignore :104 (both mandatory) + DI AddScoped :66, all adjacent to Item lines. Informational only (non-blocking): (a) explicit non-unique HasIndex on ItemId/UomId (:50-51) redundant with EF convention FK indexes — zero migration impact; (b) controller Enum.Parse unguarded for invalid posted string → 500, matches PaymentMethod precedent exactly (pre-existing pattern).
CLEAN — G2 PriceList + ItemPriceList slice. All 8 axes verified: (1) CurrencyCode Option A — ctor IsNullOrWhiteSpace→DomainException, Length!=3→DomainException, then ToUpperInvariant() normalize; validator Matches("^[A-Z]{3}$") rejects lowercase; tests agree (ctor "usd"→"USD" positive, empty/2-char/4-char throw, validator lowercase fails; no leftover lowercase-throws ctor test). (2) Uniqueness — ItemPriceList FULL unique (CompanyId, PriceListId, ItemId, CurrencyCode, EffectiveFrom) no HasFilter; PriceList full unique (CompanyId, Code). (3) Effective-dating — EffectiveFrom required, EffectiveTo? nullable, ctor+validator guard EffectiveTo >= EffectiveFrom, AddAsync-only. (4) Money — UnitPrice decimal(18,2) HasPrecision, guard >= 0, zero float. (5) Deactivate — both THROW on missing, Restrict FKs, soft-delete only, child rows survive. (6) Pattern compliance — 5 layers, CompanyId FIRST, minimal events, thin controllers, wiring item-adjacent. (7) Fakes exact (4 + 3 members). (8) Tests 15 + 26 = 41 Facts all spec'd, zero TODOs, focused run 41/41 PASS live. No migration scaffolded (snapshot clean). No WARN/BLOCK.
## Last Verifier Result
VERIFIED_PASS ×8 — G2 batch (4) + G3 (4). UomClass: 24/24; ItemGroup: 19/19; ItemBarcode: 18/18; PriceList: 41/41; ItemReorderLevel: 19/19; ItemSupplierPrice: 26/26; ItemTaxClass: 19/19; WarehouseLocation: 20/20. All gates: build 0/0, arch 22/22, BankTests full 301 (115 baseline + 186 new). ItemReorderLevel: two partial uniques warehouse_id IS [NOT] NULL AND is_active; ItemSupplierPrice: full unique 5-col NO HasFilter + Option A currency; ItemTaxClass: full unique 4-col NO HasFilter, effective-dating; WarehouseLocation: full unique (CompanyId,WarehouseId,Code) NO HasFilter, BankBranch precedent; all handlers repo-only deps. No migrations scaffolded (G4 owns #19).
## Documented Limitations (from audits, for G5)
- ItemController.Create NOT extended with optional itemGroupId param — ItemGroupId reachable via command only (research-sanctioned). UomController same question (UomClassId via Uom create form).
- ItemBarcode controller Enum.Parse unguarded for invalid posted string → 500 (PaymentMethod precedent, pre-existing pattern).
- ItemBarcodeConfiguration explicit non-unique HasIndex on ItemId/UomId redundant with EF FK indexes (zero migration impact).
## Active Heartbeats
executor: G3 ItemSupplierPrice — 15 files written, doing 3 wiring edits (re-reading shared files)
auditor: audit of G2 PriceList + ItemPriceList slice COMPLETE — CLEAN (8 axes, focused 41/41 live, gates taken from orchestrator central run)
auditor: audit of G2 ItemGroup slice COMPLETE — WARN informational (ItemController.Create itemGroupId limitation → G5 report; fact-count typo only)
auditor: audit of G2 UomClass slice COMPLETE — CLEAN (§7b 7-file surface exact, class-compat null-safe, fakes trap avoided, wiring adjacent)
auditor: audit of G2 ItemBarcode slice COMPLETE — CLEAN (GS1 Mod-10 verified by hand all 4 types, two-flag HasFilter exact, 18/18 Facts, gates re-run 0/0 + 22/22 + 217/217)
executor: G2 ItemBarcode slice — all 20 files written, arch 22/22 PASS, GS1 vectors verified; full build blocked on ItemGroup executor's transient CS0246 in ItemGroupAggregateTests.cs (not my file), retrying
executor: PriceList slice — 30 files written + wiring done, building now
executor: G2 UomClass slice — building 16 new + 11 modified files (Domain → Uom append → Infra → App → Api → Tests → Wiring), then focused tests
researcher: G2 ItemBarcode slice research done — GS1 Mod-10 verified by execution (positive+negative vectors), PaymentMethod enum-in-controller precedent, DB/DI wiring (:40/:96/:63), spec RESEARCH.md:1114-1542
researcher: G2 PriceList slice research done — 2-entities-1-slice, currencyCode contradiction resolved (Option A), spec RESEARCH.md (Task-Specific Research — PriceList Slice)
researcher: G2 ItemGroup slice research done — 7-file append surface verified (CreateItemHandler pass-through gap flagged), C1 confirmed, 18 Facts spec'd, wiring lines captured, spec appended to RESEARCH.md
researcher: G2 UomClass slice research done — §7b verified zero-discrepancy, Fakes.cs gap (3 new fakes), class-compat null-safe recommendation, wiring line numbers captured, spec appended to RESEARCH.md
researcher: G1 design deep-dive written — 5 conflicts resolved + 8 entity design blocks appended to RESEARCH.md, reconciled against R1–R32, done
executor: G1 transcription complete — "## Design Decisions (G1)" appended to RESEARCH.md (8 entity blocks + C1–C5 + R1–R32 matrix + dependency map + 6-file UomClassId surface + GTIN block), STATUS.md updated, done
## Blocked Reason
(none)
researcher: G3 ItemSupplierPrice slice research in progress — reading templates (SupplierItem/ItemPriceList/wiring)
researcher: G3 ItemSupplierPrice spec done — 15 CREATE + 3 wiring, 26 Facts, wiring :46/:108/:69/:405, ItemPriceList template, full-unique no HasFilter, no src edits
