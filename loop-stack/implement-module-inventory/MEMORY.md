# Loop Memory
Updated continuously by all agents as they discover things.
## Learnings

### G1 Design Reconciliation — 2026-09-24 (executor)
- **Deliverable shape:** "## Design Decisions (G1)" appended to RESEARCH.md = §0 cross-cutting canon + §1–§8 per-entity blocks + §7b 6-file UomClassId append surface + §9 C1–C5 citations + §10 R1–R32 disposition matrix + §11 dependency map. G2/G3 executors follow verbatim.
- **Verified source facts used in transcription (all re-read, not just researcher-cited):** Uom ctor `(companyId, code, name, symbol=null, description=null)` — 5 positional args at CreateUomHandler.cs:15, so UomClassId param LAST compiles unchanged; Supplier.cs:31-32 `HasValue && <= 0` guard + property-before-IsActive placement (SupplierGroupId at :17); Item ctor has NO ItemGroupId yet — append is param LAST; SupplierItem port = 3 members (GetByIdAsync/GetAllByCompanyAsync/AddAsync, NO GetByCodeAsync) — child-shape template; TaxRate unique `(CompanyId, TaxTypeId, RateValue, EffectiveFrom)` full unique = effective-dated versioning-key template; BankBranch unique `(CompanyId, BankId, Code)` = parent/child master template; UomConversionConfiguration.cs:24 full unique triple + Deactivate() = the R1 dead-end (documented limitation, no ALTER in #19).
- **GS1 Mod-10 verified by hand:** GTIN-13 `6291041500213` (data 629104150021, weights 3/1 right-to-left from rightmost data digit, sum=57, cd=3 ✓) and `5012345670003` (sum=57, cd=3 ✓) — both test vectors correct; algorithm `checkDigit = (10 - (sum % 10)) % 10`.
- **Money VO trap confirmed:** Money.cs:13 throws ArgumentNullException (not DomainException) + no ISO validation → new price entities use scalar decimal(18,2) + string CurrencyCode(3), NOT the VO (R23/R24).
- **ItemReorderLevel NULL trap:** nullable WarehouseId + single full-unique triple = unlimited NULL-warehouse rows (Postgres NULLs distinct) → TWO partial indexes: `(CompanyId, ItemId)` WHERE `warehouse_id IS NULL AND is_active` + `(CompanyId, ItemId, WarehouseId)` WHERE `warehouse_id IS NOT NULL AND is_active`.
- **ItemTaxClass:** TaxTypeId-only (no TaxRateId — R16 date-axis duplication; no AccountId — R15 TaxAccountingMapping complement). Unique `(CompanyId, ItemId, TaxTypeId, EffectiveFrom)`.
- **Migration #19 shape:** 9 CreateTable + 2 nullable AddColumn NO defaultValue (item_group_id on items, uom_class_id on uoms) + 4 HasFilter partial uniques (item_barcodes is_active + is_primary; item_reorder_levels 2× warehouse_id IS [NOT] NULL AND is_active) + all FKs Restrict. Check-only.
- **G1 is loop-state only:** zero src/ changes, zero migrations, zero git staging — no commit made (executor rule: git commit only if git enabled AND task produces code; G1 explicitly "Do NOT stage anything").
- **Deactivate command shape:** `DeactivateXxxCommand(long Id)` (no CompanyId) — DeactivateItemCommand.cs precedent; result `(bool Success)`.
- **Validator currency rule:** `NotEmpty().Length(3).Matches("^[A-Z]{3}$")`; domain ctor normalizes `ToUpperInvariant()` (ExchangeRate.cs:40 precedent).
- **EffectiveTo rule:** domain guard + validator Must: `!EffectiveTo.HasValue || EffectiveTo >= EffectiveFrom`.

### G1 Design-Session Learnings — 2026-09-24 (memory-keeper)
- **C1–C5 resolutions (canonical, G2/G3 follow verbatim):** C1 ItemGroup = NEW flat master (NO ParentId — hierarchy stays in ItemCategory) + nullable ItemGroupId append on Item (param LAST, HasValue&&<=0 guard, AddColumn NO defaultValue); C2 ItemTaxClass = link Item↔TaxType ONLY (no TaxRateId — R16 date-axis duplication; no AccountId — R15 TaxAccountingMapping complement), unique (CompanyId,ItemId,TaxTypeId,EffectiveFrom); C3 ItemSupplierPrice = NEW price entity (UnitPrice decimal(18,2), CurrencyCode string(3), effective-dated, unique (CompanyId,SupplierId,ItemId,CurrencyCode,EffectiveFrom)), SupplierItem untouched; C4 UomClass = new master + nullable UomClassId append on Uom + class-compat check in CreateUomConversionHandler (Application layer — entity has no repo access, cannot validate at construction; reject ONLY when both classes present and unequal); C5 ValuationMethod LIFO/SpecificIdentification = document-only, NO enum mutation (string storage → adding SpecificIdentification later is additive/migration-free, removing LIFO is breaking).
- **HasFilter dead-end refinement (auditor-caught):** partial-unique filter must include ALL mutable flags Deactivate() doesn't reset — item_barcodes primary-unique `"is_primary"` alone recreates the deactivate→re-add dead-end (Deactivate() sets IsActive=false but does NOT clear IsPrimary); canonical = `HasFilter("\"is_primary\" AND \"is_active\"")` (RESEARCH.md:714). Extends SupplierItem lesson (MEMORY:263) to multi-flag entities.
- **Transcription-drop failure mode (auditor Issue C → verifier):** canonical §4 originally DROPPED the handler null-guard from the deep-dive — fix propagated to 3 cross-referencing spots (canonical block :850, BankTests list :862, R25 row :1088); G2/G3 verification must check canonical-record vs deep-dive sync AND all cross-referencing sections (block + tests list + R-matrix row + §0 migration shape) when a spec is corrected.
- **GS1 Mod-10 spec (canonical):** GTIN-8/12/13/14 — last digit = check digit, data digits right-to-left from rightmost, weights 3/1 with 3 on rightmost data digit, `checkDigit = (10 - (sum % 10)) % 10`; verified vectors 6291041500213 (sum=57, cd=3) + 5012345670003 (sum=57, cd=3); validator-only (skip for Other, R21), length-match enforced inside; no UPC-E (store as GTIN-12).
- **UomClassId 6-file append surface (R22):** Uom.cs ctor param LAST + HasValue&&<=0 guard + property BEFORE IsActive (Supplier.cs:17 precedent); CreateUomCommand param LAST; validator `GreaterThan(0).When(HasValue)`; UomDto field before IsActive; GetUomHandler Map; UomConfiguration column + HasOne<UomClass> Restrict. Only callsite CreateUomHandler.cs:15 (5 positional args) compiles unchanged; uoms table 0 live rows = zero backfill risk.
