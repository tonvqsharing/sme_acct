# Loop Plan
## Mode
build
## Goal
Implement Module 03 - Inventory & Items for SME Accounting: Item, ItemCategory, ItemGroup, ItemBarcode/GTIN, ItemPriceList/PriceList, ItemReorderLevel, ItemSupplierPrice, ItemTaxClass, Uom, UomConversion, UomClass, Warehouse, WarehouseLocation/Bin, InventoryValuationPolicy, InventoryAccountingConfiguration, InventoryAdjustmentReason. Master-data and configuration foundation only - no warehouse transaction engine, purchasing module, sales module, or stock ledger unless existing capabilities prove integration required. Preserve project identity. Discover first, trace existing design, implement smallest correct slice, TDD, evidence-based PASS.
## Stop Condition
all tasks in loop-stack/implement-module-inventory/PLAN.md checked
## Budget
20 turns
## Git Integration
yes
## Tasks
11 tasks / 5 groups. Structure follows goal-context proposal (parallel executor granularity overrides generic 3-7 heuristic). G1 design gates all slices; G2/G3 slices write code+tests only (NO migrations — avoids parallel snapshot races); G4 consolidates migration #19 + regression; G5 reports. Commit rule for ALL tasks: stage ONLY Module 03 files, never `git add -A` — ~69 uncommitted business-partners files must stay unstaged; check `git status` before every commit.

- [x] [G1] Design reconciliation for 8 missing inventory entities — resolve the 5 open conflicts, each with explicit decision + rationale + consequence: (1) ItemGroup vs ItemCategory roles (no duplicate hierarchy — distinct roles or justified absorb); (2) ItemTaxClass shape (link Item↔TaxType/TaxRate with effective-dating vs FK on Item); (3) ItemSupplierPrice (new price-bearing entity referencing Supplier+Item vs extending SupplierItem); (4) UomClass (nullable UomClassId append on Uom per business-partners nullable-FK canon: AddColumn nullable NO defaultValue, ctor param LAST, + conversion class-compatibility validation vs document-only); (5) ValuationMethod LIFO/SpecificIdentification — lean document-only known-limitation, no enum mutation without user approval. Finalize field/scope/uniqueness/effective-date design for all 8: ItemGroup, ItemBarcode/GTIN (incl. GTIN check-digit Mod-10 weights 3/1 per GS1 spec in TOOLS.md, uniqueness scope), PriceList+ItemPriceList (decimal + string Currency per Money VO), ItemReorderLevel, ItemSupplierPrice, ItemTaxClass, UomClass, WarehouseLocation/Bin. Append "## Design Decisions (G1)" to loop-stack/implement-module-inventory/RESEARCH.md. No code changes. G2/G3 executors follow these decisions verbatim.
- [x] [G2] UomClass vertical slice per G1 design — 5-layer pattern from Item template (RESEARCH.md §2: entity+event+port → EF config → repo → flat CQRS command/handler/validator/query/DTO → thin controller → BankTests Facts). Tests: List-backed fakes in tests/SmeAccounting.BankTests/Fakes.cs, Id=5-before-deactivate convention, explicit `using SmeAccounting.Application.Handlers;`. If G1 decided nullable UomClassId append on Uom: ctor param LAST + `HasValue && <= 0` guard, EF HasOne<UomClass> Restrict, NO defaultValue. Wiring: DbContext Ignore line + DI AddScoped (re-read shared files before edit; insert adjacent to Uom lines). NO migration. Commit ONLY own files.
- [x] [G2] ItemGroup vertical slice per G1 design (reconciled with ItemCategory — no duplicate hierarchy) — same 5-layer pattern + tests + wiring + commit hygiene as UomClass task. NO migration.
- [x] [G2] ItemBarcode/GTIN vertical slice per G1 design (Item child; GTIN check-digit validation per GS1 Mod-10 weights 3/1 if G1 decided; uniqueness scope per G1) — same 5-layer pattern + tests + wiring + commit hygiene. NO migration.
- [x] [G2] PriceList + ItemPriceList vertical slice per G1 design (PriceList master + ItemPriceList child; decimal + string Currency per Money VO; effective-dating per G1) — same 5-layer pattern + tests + wiring + commit hygiene. NO migration.
- [ ] [G3] ItemReorderLevel vertical slice per G1 design (ItemId + WarehouseId? FKs; decimal quantities) — same 5-layer pattern + tests + wiring + commit hygiene. NO migration.
- [ ] [G3] ItemSupplierPrice vertical slice per G1 design (new price-bearing entity referencing Supplier+Item, effective-dated; do NOT modify SupplierItem) — same 5-layer pattern + tests + wiring + commit hygiene. NO migration.
- [ ] [G3] ItemTaxClass vertical slice per G1 design (link Item↔TaxType/TaxRate with effective-dating if G1 decided) — same 5-layer pattern + tests + wiring + commit hygiene. NO migration.
- [ ] [G3] WarehouseLocation/Bin vertical slice per G1 design (Warehouse child; Bank→BankBranch parent/child precedent) — same 5-layer pattern + tests + wiring + commit hygiene. NO migration.
- [ ] [G4] Consolidated migration #19 + full regression + commit hygiene — after all slices green: `dotnet ef migrations add AddInventoryModule03 --project src/SmeAccounting.Infrastructure --startup-project src/SmeAccounting.Api` (build must pass first; first ef invocation ~2min). Review generated SQL: snake_case, xmin, all FKs Restrict (never Cascade/SetNull), unique indexes per G1 design, partial-unique HasFilter for link entities, nullable AddColumn NO defaultValue for Uom append, Down() exact reversal. Check-only — never `database update` without explicit user approval (DB reachable at 172.21.208.1; if approved, apply + verify tables via psql). Full regression: build 0/0, arch 22/22, BankTests full suite (≥115 + new Facts). Commit hygiene: git status check, only inventory files staged, business-partners WIP untouched.
- [ ] [G5] Final reconciliation matrix + DoD + report — 16-component × 7-dimension matrix (Discovery/Design/Code/DB/Tests/Security/Review; 8 existing = parity-verify evidence, no code changes), 25-item DoD checklist with quoted evidence, known limitations (ValuationMethod LIFO/SpecificIdentification documented-only; G1-deferred items), evidence-based PASS/FAIL → REPORT.md in loop dir. No code changes.
