# Loop Plan
## Mode
patch
## Goal
Create canonical posting_references row for OpeningBalance flow: in PostOpeningBalancesHandler after SaveChangesAsync (JE Id assigned), build PostingReference(companyId=period.CompanyId, journalEntryId=je.Id, SourceType=OpeningBalance, sourceId=period.Id) and AddAsync via IPostingReferenceRepository in same UoW; core to edge following discovery-first TDD rules
## Stop Condition
all tasks in loop-stack/opening-balance-pr-link/PLAN.md checked
## Budget
20 turns
## Git Integration
yes
## Tasks
- [x] [G1] Write design note `docs/OpeningBalance-PRLink-Design-2026.md` locking: (1) R1 decision = **Option B, no GetBySourceAsync pre-check** — IsPosted guard (OpeningBalancePeriod.cs:61-62) blocks sequential dupes pre-persistence; xmin concurrency token on period row catches concurrent race at save 1 (DbUpdateConcurrencyException, JE never persists); unique index (CompanyId,SourceType,SourceId) last resort; CreatePostingReferenceHandler.cs:17-19 pre-check is TOCTOU-ineffective + adds round-trip + dangling-JE risk if placed post-save-1 (cite RESEARCH R1); (2) two-save shape: save 1 (:24) assigns JE Id → build `new PostingReference(period.CompanyId, journalEntry.Id, "OpeningBalance", period.Id)` → `postingReferenceRepository.AddAsync` → second `unitOfWork.SaveChangesAsync`, same UoW/DbContext, two transactions (R2); (3) FakeJournalEntryRepository.AddAsync MUST assign Id (R4 enabler, V2 guard `journalEntryId > 0`); (4) existing facts (h)/(i) SaveCalledCount==1 → ==2; (5) no-touch list: zero migration/DI/EF/controller/command changes, touched files only `PostOpeningBalancesHandler.cs` + `JournalEntrySourceTests.cs` + `Fakes.cs` (R5); (6) gates: build 0/0, arch 22/22, BankTests 57+new; (7) return value `PostOpeningBalancesResult(bool)` unchanged (R2). Cite lineage: OpeningBalance-Persist-Design-2026.md §5:77 (PR creation deferred to THIS loop), §7 same-UoW atomic write, PostingReference-Design-2026.md §7 post-JE-persist timing. Path must match PLAN exactly (posting-reference-harden G1 lesson, MEMORY:245).
- [ ] [G2] TDD handler change, core→edge: **RED** — add PR-link facts to `tests/SmeAccounting.BankTests/JournalEntrySourceTests.cs` following fact (i) shape (:191-215): set `period.Id = 1` via BaseEntity.Id public setter, run handler, assert `postingReferenceRepository.Stored` Single {CompanyId==period.CompanyId, JournalEntryId==stored JE Id (>0), SourceType=="OpeningBalance", SourceId==period.Id} and `unitOfWork.SaveCalledCount == 2`; existing facts (h) :176 and (i) :202 ctor call sites → CS1729 compile-error RED (record actual error code + lines, don't force predicted — MEMORY:254). **GREEN** — append `IPostingReferenceRepository` as 4th ctor param (end, backwards-compatible — MEMORY:74) to `src/SmeAccounting.Application/Handlers/PostOpeningBalancesHandler.cs`; after first `SaveChangesAsync` (:24) build PR with `period.CompanyId` (direct, in scope — R3), `journalEntry.Id`, `"OpeningBalance"`, `period.Id`; `AddAsync`; second `SaveChangesAsync`; return `new PostOpeningBalancesResult(true)` unchanged. Update facts (h)/(i) ctor calls to 4-param. **Enabler**: modify `tests/SmeAccounting.BankTests/Fakes.cs` `FakeJournalEntryRepository.AddAsync` (:79-83) to assign Id (counter-based, simulates EF identity) — required or PR V2 guard throws at runtime (R4); safe for existing facts (only JE Id==0 assert is G2-3 :166 pre-handler). No changes to CreatePostingReferenceHandler/Command/Controller, EF configs, DbContext, DI, migrations (R5). Follow CreatePostingReferenceHandler.cs:21-28 build/Add/save shape.
- [ ] [G3] Verify: `dotnet build SmeAccounting.sln` 0 warnings/0 errors (TreatWarningsAsErrors); `dotnet test tests/SmeAccounting.BankTests/` all green (57 existing + new PR-link facts); `dotnet test tests/SmeAccounting.ArchitectureTests/` 22/22. No-migration evidence: Migrations dir file count == 35 (17×2 + snapshot), latest `20260922084832_PostingReferenceHarden` predates loop commits, zero NEW migration files (count + `git show --stat` per loop commit — MEMORY:252, :256; grep over Migrations INVALID per G1 trap). No-touch audit: `git show --stat` per commit cross-checked against no-touch list (zero code changes ⇒ state-files-only commits). RED-reconstruction canon (MEMORY:255): `git checkout <GREEN commit>~1 -- <changed files>`, rebuild → compile-error RED reproduces deterministically, restore after. Confirm handler ctor 4-param end-appended, PR built strictly after save 1, SaveCalledCount==2 in new facts, return value unchanged.
