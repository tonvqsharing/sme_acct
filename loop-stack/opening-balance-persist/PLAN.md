# Loop Plan
## Mode
patch
## Goal
Fix OpeningBalance handler: persist JournalEntry (Add to repo, Save via UoW, use assigned Id) before linking to PostingReference; update fact (i) test in same change; core to edge following discovery-first TDD rules
## Stop Condition
all tasks in loop-stack/opening-balance-persist/PLAN.md checked
## Budget
20 turns
## Git Integration
yes
## Tasks
- [x] [G1] Write design note `docs/OpeningBalance-Persist-Design-2026.md` (exact path — auditor WARNs on mismatch). Content: (1) decision — JE persist-first: `journalEntryRepository.AddAsync(journalEntry)` then existing `unitOfWork.SaveChangesAsync` in `PostOpeningBalancesHandler`, EF assigns Id during Save, JE Id > 0 before any PostingReference link (V2 guard `journalEntryId > 0`); (2) domain surface — `OpeningBalancePeriod.PostOpeningBalances` `void`→`JournalEntry` return (JE construction stays in domain, no event change: `OpeningBalancesPosted` keeps PeriodId+CompanyId); (3) handler surface — 3rd ctor param `IJournalEntryRepository`, Add-before-Save, return unchanged; (4) test surface — fact (i) flip in `tests/SmeAccounting.BankTests/JournalEntrySourceTests.cs` to assert persistence, `FakeJournalEntryRepository` added to `Fakes.cs` (copy `FakePostingReferenceRepository` shape lines 50-67), fact (h) ctor gains fake JE repo arg; (5) no-touch list — NO PostingReference creation in this handler (PR-link is separate future task per `docs/PostingReference-Design-2026.md` §7 same-UoW atomic write), NO new command/handler/controller, NO EF/DbContext/DI change (`EfJournalEntryRepository` already registered, `DependencyInjection.cs:29`), NO migration (`journal_entries` table exists since InitialCreate); (6) gates — build 0 warnings/0 errors, arch 22/22, BankTests 54+new green, no-migration evidence pattern (Migrations count 17 + latest `20260922084832_PostingReferenceHarden` predates loop commits + zero NEW migration scaffolded; note grep -i openingbalance over Migrations returns 134 pre-existing property-name hits and is NOT valid zero-evidence — use diff/commit-tail instead). Cite `docs/PostingReference-Design-2026.md` §7/§9 and `docs/SetSource-CallerFix-Design-2026.md` §2/§5. Verify: file exists with decision + no-touch list + gates.
- [ ] [G2] Domain TDD — expose JE from `OpeningBalancePeriod.PostOpeningBalances` (`src/SmeAccounting.Domain/Entities/OpeningBalancePeriod.cs:59`). RED first: add Facts to `tests/SmeAccounting.BankTests/JournalEntrySourceTests.cs` asserting the method returns the created JournalEntry (non-null, `SourceType == "OpeningBalance"`, `SourceId == period.Id`, `IsPosted == true`, balanced lines) — confirm RED (void cannot be assigned). GREEN: change `void` → `JournalEntry`, add `return journalEntry;` after `journalEntry.Post(...)` (line 88), before/after `IsPosted=true`/`Status=Closed` (lines 90-91). Existing 2 test sites (lines 105, 119) discard the return — non-breaking, no edit needed. NO event change, domain stays zero-NuGet. Verify: build 0/0, new Facts green.
- [ ] [G3] Handler TDD — persist JE before Save in `src/SmeAccounting.Application/Handlers/PostOpeningBalancesHandler.cs` (25 lines, internal sealed, primary ctor). RED first: flip fact (i) in `JournalEntrySourceTests.cs` — rename `PostOpeningBalancesHandler_DiscardsJournalEntry_NoJournalEntryRepository` (lines 146-156) → `PostOpeningBalancesHandler_PersistsJournalEntry_AddsToRepository`; assert ctor HAS `IJournalEntryRepository` param AND handler persists JE; add `FakeJournalEntryRepository` to `Fakes.cs` (List-backed, copy `FakePostingReferenceRepository` shape lines 50-67: `GetByIdAsync`/`GetAllAsync`/`AddAsync`/`Stored`; NO `GetBySourceAsync` — port doesn't expose it); extend fact (h) (lines 128-144) ctor call with fake JE repo arg; confirm RED. GREEN: add 3rd ctor param `IJournalEntryRepository journalEntryRepository`; capture `var journalEntry = period.PostOpeningBalances(...)`; `await journalEntryRepository.AddAsync(journalEntry)` BEFORE existing `unitOfWork.SaveChangesAsync` (line 21); return `PostOpeningBalancesResult(true)` unchanged. Optional: set `journalEntry.Id = 5` post-Add in test (BaseEntity.Id public setter) to simulate EF handoff — simulates, doesn't test, EF. Assert: `Stored` exactly 1 JE, `SourceType == "OpeningBalance"`, `SourceId == period.Id`, `IsPosted == true`, `SaveCalledCount == 1`. Verify: build 0/0, BankTests green.
- [ ] [G4] Verify core→edge + no-migration statement. Run gates: `dotnet build SmeAccounting.sln` (0 warnings/0 errors, TreatWarningsAsErrors), `dotnet test tests/SmeAccounting.ArchitectureTests/` (22/22), `dotnet test tests/SmeAccounting.BankTests/` (54 pre-existing + new Facts green). No-migration evidence: Migrations dir count = 17, latest = `20260922084832_PostingReferenceHarden` predates loop commits, zero NEW migration scaffolded this loop (compare commit-tail/diff — NOT grep, which yields 134 pre-existing property-name hits) No-touch audit: grep confirms `PostOpeningBalancesHandler` contains no PostingReference creation, no DI/EF/controller/command changes, no migration added. Record results (gates + evidence) in loop STATUS.md and REPORT.md.
