# Loop Memory
Updated continuously by all agents as they discover things.
## Learnings
- [G1] PaymentMethod design doc written to docs/PaymentMethod-Design-2026.md (164 lines, 0 placeholders): PaymentTerm 1:1 copy with PaymentTermType/Days swapped for PaymentMethodCategory/RequiresBankAccount(bool default false); enum PaymentMethodCategory (Cash/BankTransfer/Card/EWallet/Other) in Domain/ValueObjects with HasConversion<string>; constraints composite unique (CompanyId,Code) + Company FK Restrict + xmin + snake_case payment_methods; no Bank/Journal FKs; DomainException + FluentValidation matrix (20/200/500 + IsInEnum); UNKNOWN-1..4 resolved, UNKNOWN-5/6 carried forward.
- [G1 verified] Design locks for G2: enum `PaymentMethodCategory` in Domain/ValueObjects (never Domain/Enums) with members Cash/BankTransfer/Card/EWallet/Other string-stored; `RequiresBankAccount bool default false` scalar over nullable BankAccountId FK (one method spans many accounts); category column locked to `category` (UNKNOWN-6 resolved); lengths Code 20 / Name 200 / Description 500; composite unique (CompanyId,Code) + Company FK Restrict + xmin last; ctor `(companyId,code,name,category,requiresBankAccount=false,description=null)` with DomainException + PaymentMethodCreated(Id,CompanyId,UtcNow) minimal event; Ignore-only DbContext wiring (no DbSet) + single AddScoped DI; auditor WARN 2 minors fixed pre-verify (PLAN path → docs/PaymentMethod-Design-2026.md, category column locked).
- [G2] Slice implemented discovery-first TDD, FULL GREEN 2026-09-22: RED PaymentMethodAggregateTests.cs (14 Facts) + FakePaymentMethodRepository appended to BankTests Fakes.cs reusing FakeUnitOfWork (no new project, no csproj/sln edits — InternalsVisibleTo BankTests already covered internal handlers); Domain 4 (entity+PaymentMethodCategory+event+port, file-scoped namespaces, Domain zero refs); Application flat layout 10 (Create/Deactivate commands+handlers, GetById/GetByCompany queries+handlers, CreateValidator with IsInEnum, single-record PaymentMethodDto with Category string); Infra config (ToTable payment_methods, category HasConversion<string>, requires_bank_account, unique (CompanyId,Code), Restrict, xmin last) + EfPaymentMethodRepository via Set<T>; wiring exactly 1 Ignore line + 1 AddScoped line; Api PaymentMethodController thin MediatR (no Domain.Entities using) + CreatePaymentMethodViewModel; invalid category covered at validator IsInEnum only (PaymentTerm-template faithful, no Enum.IsDefined domain guard); verify: build 0 warn 0 err, BankTests 27/27 (13 old + 14 new), arch 22/22.
