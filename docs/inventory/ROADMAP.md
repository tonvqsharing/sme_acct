# Roadmap & Execution Plan — Inventory to Full PROD

## Gantt (5 sprints, 2w each)

```
W1-2   W3-4   W5-6   W7-8   W9-10
├──────┼──────┼──────┼──────┤
│ S1   │ S2   │ S3   │ S4   │ S5   │ hardening
│Move+ │Cost  │NRV+  │Count │Reports integration
│Loc   │method│152/56│lock  │NXT/turnover
```

## Sprint breakdown

### S1 — Product/Location/Move/Shipment/Period (2w)

```
T1 domain: Product(code,UOM,cost_method,standard), Location(warehouse hierarchy, VIRTUAL), StockMove(qty,cost,from→to,effective_date), Shipment
T2 storage: 5 tables + stock_periods
T3 service: create + FY+period gates + qty>0 + post → DONE + audit+checksum
T4 web: 5 endpoints + AUDITOR 403
T5 tests: UT product/location/move/shipment states, integration PN→DONE stock 100
Verify: pytest 988+ → 1000+, mypy, black
```

### S2 — Cost method per product (2w)

```
T1 implement wavg moving, fifo queue, specific lot pick, standard + variance
T2 CostRevision wizard: ModifyCostPrice → revision at effective_date
T3 integrate sales CUSTOMER_OUT → 632 per method (replace stub 632)
T4 purchase SupplierShipment → 152 direct (remove 611)
T5 tests: UT each method, UT mixed per SKU, integration in 100@10k out 30 per method
```

### S3 — NRV + COA 152/156 mapping (1w)

```
T1 provision 229: NRV < cost → Nợ 632/Có 229
T2 long-term material >12m → not inventory but asset (TT99 note)
T3 migration: grep 611 → 0, replace with 152/156 via moves
T4 B01 note disclosure: HTK provision impact
```

### S4 — Count + lock + audit (1w)

```
T1 Inventory count wizard: counted vs expected → adjustment moves to VIRTUAL
T2 Stock period CLOSED blocks moves (409)
T3 10y checksum + audit_log for every move/shipment/period
T4 SOD: chief for cost change, period close
```

### S5 — Reports + integration (2w)

```
T1 GET /inventory/stock (warehouse+as_of + pagination)
T2 NXT (nhập-xuất-tồn) per product/warehouse
T3 Thẻ kho per product/location
T4 Turnover
T5 Integration: trial-balance 152 reconciles stock value; B01/B02 includes 152/632
```

## Order rationale (why S1 first)

S1 is smallest slice proving location hierarchy + move state + FY gate tracer bullet. S2 largest (cost algorithms) needs stable move foundation. S3-S4 sequential after cost. S5 reports need all prior.

## Execution checklist per PR

```
uv run ruff check src tests
uv run black --check src tests
uv run mypy --ignore-missing-imports src/bricks/<brick>
uv run pytest -q  # + new inventory tests
git commit feat(inventory):
```

## Risk & mitigation

| Risk | Mitigation |
|---|---|
| Cost method complexity per SKU | Start wavg only, add fifo/specific/standard one by one with UT per method |
| 611 migration missed | grep lock CI + review autogenerated alembic: replace JSONType with sa.Text |
| Oversell race | row lock on stock_period + check stock before DONE in same transaction |
| Period lock missed | Wire SystemSettings period_lock same adapter as sales/financial_statements |

## Done definition PROD

```
Full inventory PROD = S1-S5 closed + NXT==stock_value==trial 152 + no 611
                    + turnover correct + period lock blocks + 10y audit
Feature flag inventory.enabled → true.
Runbook + migration 611→152 tested.
```
