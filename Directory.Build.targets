<Project>

  <!--
    Placeholder for TFM-conditioned targets.
    Rule: conditional logic that needs final (post-SDK-evaluation) values
    (e.g. $(TargetFramework)) belongs here, NOT in Directory.Build.props,
    where the TFM is still empty during evaluation.
  -->

</Project>