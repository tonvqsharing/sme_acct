[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act, eft

[role_definition]
g = _, _

[matchers]
m = r.sub == p.sub && r.obj == p.obj && r.act == p.act

[eft]
e = some(where (p.sub == r.sub && p.obj == r.obj && p.act == r.act))
